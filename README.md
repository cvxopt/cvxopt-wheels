# cvxopt-wheels

This repository is used to build CVXOPT wheels for macOS and Windows. The macOS wheels are built with `cibuildwheel` using GitHub Actions, while the Windows wheels are built using Appveyor.